math

built-in funcs/operators/module

- operators
    - '//' returns int (1)
    - '/' returns float
    - '**' for exponentation
- built-in funcs
    - min(iterables, key=func, default=obj) (2)
        - single iterable as arg: returns its smallest elem
        - two or more args: returns smallest arg
        - key: mapping func (optional)
        - default: if iterables is empty, returns its val (optional)
            - if named arg is not given, throws ValueError
    - round(number, decimal) rounds down (3)
        - 1st arg: float to be rounded
        - 2nd arg: how many decimal places
            - if omitted, defaults to zero
            - if negative, determines how many places before comma
    - sum(iterable, init)
        - 1st arg: iterable
            - if empty, returns init
        - 2nd arg: initial val (optional)
            - if not given, defaults to zero
- methods in module 'math' (4)
    - ceil() rounds up

EXAMPLE
(1)
print(7 // 4)               # 1

(2)
print(min([1, 2, 3]))       # 1
print(min(1, 2, 3))         # 1
print(min([3, 4], [1, 2]))  # [1, 2]
res = min([1, 2, 3], key=lambda x: x if x % 2 == 0 else 10)
print(res)                  # 2

(3)
res = round(333.333, -2)    # 300.0

(4)
import math

math.ceil(333.333)          # 334
