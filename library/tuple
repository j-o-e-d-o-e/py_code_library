tuple

data structure

(1) may be concatenated
(2) may be sliced
(3) may be assigned to
- extra comma allows for tuple with single val

EXAMPLE
(1)
t = ('joe', 'doe')
print(t + ("bow",))     # ('joe', 'doe', 'bow')

(2)
print(t[::-1])          # ('doe', 'joe')
print(t[1:])            # ('doe',)

(3)
(a, b) = t
(x, y) = (t[1], t[0])
print(b, a)             # doe joe
print((b, a) == (x, y)) # True
