join

from lists to strings

S.join(list)
Returns a string that combines the strings in a list. The separator between the elements is S.

EXAMPLE
spam = ["cats", "dogs", "mice"]
" ".join(spam) #prompts: 'cats dogs mice'
print(" ".join(spam)) #prompts, of course: cats dogs mice
