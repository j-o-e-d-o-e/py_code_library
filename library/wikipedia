lib->wikipedia



- methods
    - search(query) returns list with matching entry titles
    - summary(entry) returns a summary

EXAMPLE
import wikipedia as wiki

res = wiki.search("Leprous")
print(res[:4])
# ['Leprous', 'Malina (album)', 'The Congregation (Leprous album)', 'Aphelion (Leprous album)']
print(wiki.summary(res[1])[:80])
# Malina is the fifth studio album by Norwegian progressive metal band Leprous, re
