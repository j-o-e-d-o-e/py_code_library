regex

built-in module

- methods
    - compile(pattern, flags) creates and returns a pattern obj
    - search(pattern, str, flags) searches for matches and returns match obj
    - match(pattern, str, flags) gets the matched strings
    - findall(pattern, str, flags) returns list of matches

EXAMPLE
import re

input = 'hello 12 hi 89. Howdy 34'
pattern = '\d+'

res = re.findall(pattern, input)
print(res)  # ['12', '89', '34']
