all

built-in func

- returns True if all elems of an iterable are true

EXAMPLE
slic = "racecar"
res = all(slic[i] == slic[len(slic) - 1 - i] for i in range(int(len(slic) / 2)))
print(res) # prompts: 'True', since "racecar" is a palindrome
