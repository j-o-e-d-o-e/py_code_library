import



to import modules
    (1) import 'module'
    (2) from 'module import 'function' as 'name'

EXAMPLE
(1)
import copy

a = copy.copy([1, 2, 3])
(2)
from datetime import datetime as dt

millennium = dt(2000, 1, 1)
today = dt.today()
delta = today - millennium
print(f"{delta.days} days passed since millennium.")
