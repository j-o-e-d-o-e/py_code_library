lib->send2trash

3rd party module, delete files/folders

Sends folders and files to computer's trash bin instead of permanently deleting them

EXAMPLE
import send2trash

baconFile = open('bacon.txt', 'a') # creates the file
baconFile.write('Bacon is not a vegetable.')
baconFile.close()

send2trash.send2trash('bacon.txt')
