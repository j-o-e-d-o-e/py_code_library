enum

built-in module

- iterable collection of constants
- creation via
    (1) class syntax
    (2) functional syntax

EXAMPLE
from enum import Enum
(1)
class Color(Enum):
    RED = 1
    GREEN = 2
    BLUE = 3
(2)
Color = Enum('Color', ['RED', 'GREEN', 'BLUE'])

print(Color["RED"].value)   # 1
for c in Color:
    print(c, c.value)       # Color.RED 1
                            # Color.GREEN 2
                            # Color.BLUE 3
