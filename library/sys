sys

built-in module

- properties
    - argv returns list containing program's filename [0] and args from command line [1:]

EXAMPLE
# python3 <filename> <address>
import sys
import webbrowser

address = '+'.join(sys.argv[1:])
webbrowser.open('https://www.google.com/maps/place/' + address)
